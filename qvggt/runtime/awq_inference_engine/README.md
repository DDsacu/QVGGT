# awq_inference_engine

This directory vendors the CUDA extension source required by the AWQ inference runtime.

Build it with:

```bash
cd qvggt/runtime/awq_inference_engine
pip install -e .
```

The extension name is `awq_inference_engine`.
